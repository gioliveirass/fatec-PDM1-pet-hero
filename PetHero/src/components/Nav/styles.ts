import {View} from 'react-native';
import styled from 'styled-components';

export const Container = styled(View)`
  width: 100%
  height: 70px
  margin: 24px 0

  flexDirection: row
  alignItems: center
`;
